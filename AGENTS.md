Lockfiles must be consistent with package metadata. After any change to `pixi.toml`, run `pixi lock`.

Everything runs in a Pixi environment. Prefix all commands (like `meson`) with `pixi run` (e.g. `pixi run meson`).
